import 'package:flutter/material.dart';

/// IntraZero 2026 Color Palette - Matching Standup Features Design
class IntraZeroColors {
  // Primary Gradients (Matching Standup Headers)
  static const primaryGradient = LinearGradient(
    colors: [Color(0xFF667eea), Color(0xFF764ba2)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );
  
  static const morningGradient = LinearGradient(
    colors: [Color(0xFF667eea), Color(0xFF764ba2)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );
  
  static const progressGradient = LinearGradient(
    colors: [Color(0xFF11998e), Color(0xFF38ef7d)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );
  
  static const eveningGradient = LinearGradient(
    colors: [Color(0xFFf093fb), Color(0xFFf5576c)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );
  
  static const completeGradient = LinearGradient(
    colors: [Color(0xFF4facfe), Color(0xFF00f2fe)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );
  
  static const urgentGradient = LinearGradient(
    colors: [Color(0xFFff416c), Color(0xFFff4b2b)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );
  
  // Accent Colors
  static const success = Color(0xFF10b981);
  static const warning = Color(0xFFf59e0b);
  static const danger = Color(0xFFef4444);
  static const info = Color(0xFF3b82f6);
  
  // Neutrals (From Standup Design)
  static const background = Color(0xFFF8FAFC);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceLight = Color(0xFFF1F5F9);
  static const textPrimary = Color(0xFF1e293b);
  static const textSecondary = Color(0xFF64748b);
  static const textTertiary = Color(0xFF94a3b8);
  
  // Borders & Dividers
  static const borderLight = Color(0xFFe2e8f0);
  static const borderMedium = Color(0xFFcbd5e1);
  
  // Dark Mode
  static const darkBackground = Color(0xFF0f172a);
  static const darkSurface = Color(0xFF1e293b);
}

